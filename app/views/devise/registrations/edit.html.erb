<div class="w-100 d-flex flex-column align-items-center">
  <h2 class="my-4 display-6"><%= t('password.edit') %></h2>

  <%= render partial: 'partials/registration', locals: {model: @user} %>

  <div class="col-md-3 col-12">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, data: { turbo: false } }) do |f| %>
      
      <div class="field mb-4">
        <%= f.label :current_password, class: 'form-label' %>
        <%= f.password_field :current_password, autocomplete: "current-password", class: 'text-muted form-control', 
              placeholder: t('password.confirm_changes') %>
      </div>

      <div class="actions mt-3 ">
        <%= f.submit t('password.update'), class: 'btn btn-outline-primary' %>
      </div>
    <% end %>

    <h3 class="my-3"><%= t('password.cancel_account') %></h3>

    <p><%= t('password.question') %><%= button_to t('password.cancel_account'), registration_path(resource_name), 
                    method: :delete, form: { data: { turbo_confirm: t('alert.are_you_sure') } }, class: 'btn btn-outline-danger' %></p>
  </div>

  <%= link_to t('app.back'), :back %>
</div>
