<h1 class="mb-4 display-6"><%= t('event.index') %></h1>

<div class="buffer-both">
  <%= link_to t('event.create'), new_event_path, class: 'btn btn-primary' %>
</div>

<div class="row buffer-bottom">
  <%= render partial: 'events/event', collection: @events %>
</div>
